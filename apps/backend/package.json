{
    "name": "backend",
    "version": "1.0.0",
    "description": "FastAPI backend server",
    "scripts": {
        "dev": "uvicorn server.app:app --reload --port 8000",
        "start": "uvicorn server.app:app --port 8000",
        "build": "PYTHONPATH=$(pwd)/../../ pytest --verbose || echo 'No tests found, skipping...'"
    },
    "dependencies": {},
    "devDependencies": {
        "cross-env": "^7.0.3"
    }
}
